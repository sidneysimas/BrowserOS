# Startup ports, CDP port, and resources come from the sidecar JSON passed
# with --config. Copy config.sample.json only when running the server manually.

# Optional BrowserOS state root override.
# BROWSEROS_DIR="~/.browseros-dev"

# Legacy per-agent MCP route gate. Leave disabled unless testing migration paths.
# COCKPIT_LEGACY_PER_AGENT_MCP="0"
